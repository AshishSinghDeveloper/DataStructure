using System;
namespace AmazonStack
{
    public class SortedArrayMedian
    {
        public double FindMedianSortedArrays(int[] nums1, int[] nums2)
        {
            int totalElements = nums1.Length + nums2.Length;
            int[] mergedArray = new int[totalElements];
            while(mergedArray != null)
            {
                //if()
            }
            int median1 = totalElements / 2;
            double answer = 0;
            if (totalElements % 2 != 0)
            {
                _ = median1 + 1;
            }

            //nums1.
            return answer;
        }
    }
}
